apiVersion: v1
kind: Service
metadata:
  name: rag-support-assistant
spec:
  type: ClusterIP
  selector:
    app: rag-support-assistant
  ports:
    - name: http
      port: 80
      targetPort: 8080
